# 启动图（iOS）

- 浅色：`assets/splash/splash_light.png`
- 深色：`assets/splash/splash_dark.png`
- 资源集：`ios/Runner/Assets.xcassets/LaunchImage.imageset`（含 dark appearance）
- 布局：`LaunchScreen.storyboard` 全屏 `scaleAspectFill`

更新设计稿后，替换 `assets/splash/` 下 PNG，重新生成 1x/2x/3x 并写入 `LaunchImage.imageset`。
