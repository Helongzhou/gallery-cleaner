# 整理历史时间线 & 足迹海报地图（v1.1）

**状态：已实现**

## 整理历史

- 按 `session_id` 分组，最近 20 次整理会话
- 时间线节点：时间 · 来源→目标 · 整理/待删统计
- 点击节点 → 抽屉内二级展开照片列表
- 逐条「反悔」保留

## 足迹海报

- 分享时渲染专用 `FlutterMap`，`fitBounds` 全览 + pin 圆点
- 跟随用户足迹地图样式（含「跟随系统」）
- 瓦片加载后 `RepaintBoundary` 截图嵌入海报
